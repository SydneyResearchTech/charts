apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omero-server.fullname" . }}-test-connection"
  labels:
    {{- include "omero-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "omero-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
