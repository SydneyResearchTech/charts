{{- if .Values.master.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryosparc.fullname" . }}-m-test-connection"
  labels:
    {{- include "cryosparc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryosparc.fullname" . }}:{{ .Values.master.service.port }}']
  restartPolicy: Never
{{- end }}
