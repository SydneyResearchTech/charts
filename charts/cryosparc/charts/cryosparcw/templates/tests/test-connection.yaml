apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cryosparcw.fullname" . }}-test-connection"
  labels:
    {{- include "cryosparcw.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cryosparcw.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
